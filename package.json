{
  "name": "design-course-backend",
  "version": "1.0.0",
  "description": "Backend for graphic design course platform",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "prisma generate",
    "migrate": "prisma migrate deploy",
    "postinstall": "prisma generate",
    "init-db": "node scripts/init-db.js",
    "render-start": "npm run build && npx prisma migrate dev --name init && npm run init-db && npm start"
  },
  "keywords": ["design", "course", "education"],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@prisma/client": "^5.14.0",
    "bcrypt": "^6.0.0",
    "cors": "^2.8.6",
    "dotenv": "^17.3.1",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3",
    "pg": "^8.11.3"
  },
  "devDependencies": {
    "nodemon": "^3.0.0",
    "prisma": "^5.14.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
